import React, { useState } from 'react';

function TechGlobeSection() {
  const [isPlaying, setIsPlaying] = useState(false);

  return (
    <section className="relative py-24 px-4 sm:px-6 lg:px-8 overflow-hidden" style={{ background: 'linear-gradient(180deg, #0a0a0f 0%, #0d0d1a 40%, #0a0a0f 100%)' }}>
      {/* Ambient glow effects */}
      <div className="absolute inset-0 overflow-hidden pointer-events-none">
        <div className="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 w-[800px] h-[500px] rounded-full" style={{ background: 'radial-gradient(ellipse, rgba(56, 97, 251, 0.08) 0%, rgba(139, 92, 246, 0.04) 40%, transparent 70%)' }} />
        <div className="absolute top-0 left-0 w-full h-px" style={{ background: 'linear-gradient(90deg, transparent, rgba(56, 97, 251, 0.2), transparent)' }} />
        <div className="absolute bottom-0 left-0 w-full h-px" style={{ background: 'linear-gradient(90deg, transparent, rgba(139, 92, 246, 0.2), transparent)' }} />
      </div>

      <div className="relative max-w-5xl mx-auto">
        {/* Title */}
        <div className="text-center mb-14">
          <h2 className="text-4xl font-bold text-white mb-4">Sản Phẩm Của Chúng Tôi</h2>
          <p className="text-gray-400 text-lg">Xem demo trực tiếp sản phẩm đã triển khai</p>
        </div>

        {/* Monitor / TV Frame */}
        <div className="relative mx-auto" style={{ maxWidth: '900px' }}>
          
          {/* Outer glow */}
          <div className="absolute -inset-4 rounded-3xl opacity-60" style={{ background: 'radial-gradient(ellipse at center, rgba(56, 97, 251, 0.15) 0%, transparent 70%)', filter: 'blur(20px)' }} />

          {/* Monitor body */}
          <div className="relative rounded-2xl overflow-hidden" style={{ 
            background: 'linear-gradient(135deg, rgba(30, 30, 50, 0.9) 0%, rgba(15, 15, 30, 0.95) 100%)',
            border: '1px solid rgba(255, 255, 255, 0.08)',
            boxShadow: '0 0 60px rgba(56, 97, 251, 0.1), 0 0 120px rgba(139, 92, 246, 0.05), inset 0 1px 0 rgba(255, 255, 255, 0.05)'
          }}>
            
            {/* Top bar (browser-like) */}
            <div className="flex items-center gap-2 px-5 py-3" style={{ 
              background: 'linear-gradient(180deg, rgba(255,255,255,0.04) 0%, rgba(255,255,255,0.01) 100%)',
              borderBottom: '1px solid rgba(255, 255, 255, 0.06)'
            }}>
              <div className="flex gap-1.5">
                <div className="w-3 h-3 rounded-full bg-red-500/80" />
                <div className="w-3 h-3 rounded-full bg-yellow-500/80" />
                <div className="w-3 h-3 rounded-full bg-green-500/80" />
              </div>
              <div className="flex-1 flex justify-center">
                <div className="px-4 py-1 rounded-md text-xs text-gray-500 flex items-center gap-2" style={{ background: 'rgba(255,255,255,0.03)', border: '1px solid rgba(255,255,255,0.05)' }}>
                  <svg className="w-3 h-3 text-green-400" fill="none" viewBox="0 0 24 24" stroke="currentColor" strokeWidth={2}>
                    <path strokeLinecap="round" strokeLinejoin="round" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z" />
                  </svg>
                  gnodai.com
                </div>
              </div>
            </div>

            {/* Screen / Video area */}
            <div className="relative" style={{ aspectRatio: '16/9', background: '#000' }}>
              
              {/* ========== VIDEO EMBED ========== */}
              {/* 
                HƯỚNG DẪN: Thay URL video ở đây
                
                Option 1 - YouTube: 
                Đổi src thành: https://www.youtube.com/embed/YOUR_VIDEO_ID?autoplay=1&mute=1&loop=1&playlist=YOUR_VIDEO_ID&controls=0&showinfo=0&rel=0
                
                Option 2 - File MP4:
                Comment iframe, uncomment thẻ <video> bên dưới
              */}
              
              {/* YouTube embed */}
              <iframe
                className="absolute inset-0 w-full h-full"
                src="https://www.youtube.com/embed/YOUR_VIDEO_ID?autoplay=1&mute=1&loop=1&playlist=YOUR_VIDEO_ID&controls=0&showinfo=0&rel=0&modestbranding=1"
                title="Product Demo"
                frameBorder="0"
                allow="autoplay; encrypted-media"
                allowFullScreen
              />

              {/* MP4 embed - uncomment nếu dùng file video trực tiếp */}
              {/*
              <video 
                className="absolute inset-0 w-full h-full object-cover"
                autoPlay 
                muted 
                loop 
                playsInline
                src="https://your-domain.com/demo-video.mp4"
              />
              */}

              {/* Subtle screen reflection overlay */}
              <div className="absolute inset-0 pointer-events-none" style={{ 
                background: 'linear-gradient(135deg, rgba(255,255,255,0.02) 0%, transparent 50%)',
              }} />

              {/* Corner accents */}
              <div className="absolute top-3 left-3 w-6 h-6 border-t border-l border-white/10 rounded-tl" />
              <div className="absolute top-3 right-3 w-6 h-6 border-t border-r border-white/10 rounded-tr" />
              <div className="absolute bottom-3 left-3 w-6 h-6 border-b border-l border-white/10 rounded-bl" />
              <div className="absolute bottom-3 right-3 w-6 h-6 border-b border-r border-white/10 rounded-br" />
            </div>
          </div>

          {/* Monitor stand */}
          <div className="flex flex-col items-center mt-0">
            <div className="w-20 h-8" style={{ 
              background: 'linear-gradient(180deg, rgba(30, 30, 50, 0.8) 0%, rgba(20, 20, 40, 0.6) 100%)',
              borderLeft: '1px solid rgba(255,255,255,0.05)',
              borderRight: '1px solid rgba(255,255,255,0.05)',
            }} />
            <div className="w-40 h-2 rounded-b-lg" style={{ 
              background: 'linear-gradient(180deg, rgba(30, 30, 50, 0.6) 0%, rgba(20, 20, 40, 0.3) 100%)',
              border: '1px solid rgba(255,255,255,0.05)',
              borderTop: 'none',
            }} />
            {/* Stand shadow */}
            <div className="w-48 h-1 rounded-full mt-1" style={{ background: 'radial-gradient(ellipse, rgba(56, 97, 251, 0.15), transparent)' }} />
          </div>
        </div>

        {/* Trusted by logos / bottom text */}
        <div className="mt-16 text-center">
          <p className="text-gray-500 text-sm tracking-wide uppercase mb-6">Được tin dùng bởi nhiều doanh nghiệp</p>
          <div className="flex items-center justify-center gap-8 flex-wrap opacity-40">
            {['Client A', 'Client B', 'Client C', 'Client D'].map((name, i) => (
              <span key={i} className="text-gray-400 text-lg font-semibold tracking-wider">{name}</span>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}

export default TechGlobeSection;